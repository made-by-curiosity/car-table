import { ErrorMessage } from './RequestError.styled';

export const RequestError = () => {
  return (
    <ErrorMessage>
      Something went wrong. Refresh the page or try again later.
    </ErrorMessage>
  );
};
